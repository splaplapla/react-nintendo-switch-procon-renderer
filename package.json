{
  "name": "react-nintendo-switch-procon-renderer",
  "version": "1.0.1",
  "repository": "https://github.com/splaplapla/react-nintendo-switch-procon-renderer",
  "author": "jiikko",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:types": "tsc --declaration --emitDeclarationOnly --declarationDir './dist'",
    "release-build": "yarn run build && yarn run build:types"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^18.6.1",
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "typescript": "^4.7.4"
  },
  "peerDependencies": {
    "react": ">= 16",
    "react-dom": ">= 16"
  }
}
